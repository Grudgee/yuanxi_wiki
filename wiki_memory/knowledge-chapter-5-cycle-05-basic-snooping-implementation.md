---
name: knowledge-chapter-5-cycle-05-basic-snooping-implementation
description: 自动学习周期 8/30，记录《计算机体系结构：量化研究方法》监听协议基本实现。
---

# 知识点摘要

- 基本监听实现把处理器请求、总线事务与缓存块状态组合成状态转换，并用示例协议说明动作。
- 协议正确性来自状态机与总线序列的完整配合，不能只看单个缓存的局部状态。

# 关键细节

- 批次周期：8/30（严格串行）。
- 节号/主题：§5.2，监听协议基本实现。
- 源范围起点：PDF p.384 / 书内 p.356，OCR第20行。
- 源范围终点：PDF p.390 / 书内 p.362，OCR第9行（含）。
- 下一锚点（不含）：PDF p.390 / 书内 p.362，OCR第10行，Extensions to the Basic Coherence Protocol标题。
- OCR源字符数：17906（按实际分段文本逐字符统计，严格小于80,000）。
- OCR方法：扫描页转140 DPI JPEG，以Tesseract eng逐页识别；标题、图表讨论与章节边界同时对照页面图像。

# 核心概念

- 基本监听实现把处理器请求、总线事务与缓存块状态组合成状态转换，并用示例协议说明动作。

# 关键结论

- 协议正确性来自状态机与总线序列的完整配合，不能只看单个缓存的局部状态。

# 边界依据

- 起点采用PDF p.384 OCR第20行的已核验语义锚点；终点完整覆盖当前主题至PDF p.390 OCR第9行。
- 在下一独立标题、完整主题段或章节硬边界PDF p.390 OCR第10行前停止；下一周期从同一锚点开始，因而不切断连续正文或图表讨论。

# 原文引用

- 文档：books/计算机体系结构量化研究方法.pdf
- 版本/日期：第5版；具体印次未知。
- 位置：§5.2；PDF p.384 / 书内 p.356，OCR第20行至PDF p.390 / 书内 p.362，OCR第9行。
- 依据：上述范围的实际OCR与关键页面图像；不以常识替代扫描原文。

# 适用条件与例外

- 体系结构、处理器和性能数据按本书写作时点理解，不外推为当前产品规格。
- 状态图、表格状态名及箭头方向需引用时逐项核图。

# 关联章节

- §5.2；与周期7和周期9按锚点连续。

# 待核验问题

- 状态图、表格状态名及箭头方向需引用时逐项核图。

# 下一精确断点

- PDF p.390 / 书内 p.362，OCR第10行，Extensions to the Basic Coherence Protocol标题。
